туду:
    1   короче в App.js 2 глобальные заглушки isAdmin и isAuthorised. они передаются во все компоненты, где нужны
        проверка на админа и авторизированного пользователя, а там уже по обстоятельствам что то отрисовывается,а что то нет
    2   почему не работает перенаправление в методах, где history.push
    3   иногда если возвращаться на предыдущую страницу, отображается только навбар, хотя в адресной строке нормальный адрес
    4   а вообще, нужно ли скидывать в стор ПРЯМ ВСЕХ артистов/альбомы при соответствующих запросах?
        что-то мне подсказывает, что нет

    Response to preflight request doesn't pass access control check: No 'Access-Control-Allow-Origin' header is present on the requested resource.
    очень много с этой ошибкой связано, не пропускает ни один пост/делит запрос


